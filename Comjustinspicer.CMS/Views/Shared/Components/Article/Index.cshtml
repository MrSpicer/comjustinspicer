@model Comjustinspicer.CMS.Models.Article.ArticleListViewModel
@using Comjustinspicer.CMS.Services;
@using Comjustinspicer.CMS.Models.Article
@inject UserService UserService;

@{
	ViewData["Title"] = "Article";

	var isAuthor = UserService.IsUserAuthor;
}


<section class="section">
	<div class="container">
		<div class="columns is-multiline">
			@if (isAuthor)
			{
				<div class="mb-4">
					<a asp-action="Edit" asp-controller="AdminArticle" class="button is-primary">New +</a>
				</div>
			}
			@foreach (var post in Model.Articles)
			{
				<div class="column is-full">
					<div class="card">
						<div class="card-content">
							<p class="title">
								<a href="@Url.Action("Index", "ArticlePost", new { id = post.Id })">@post.Title</a>
							</p>
							<p class="subtitle">@post.PublicationDate.ToString("MMMM dd, yyyy")</p>
							<hr />
							@Html.Raw(post.Body)

							<div class="mt-4">
								@if (isAuthor)
								{
									<a asp-action="Edit" asp-controller="AdminArticle" asp-route-id="@post.Id"
										class="button is-primary">Edit</a>
								}
							</div>
						</div>
					</div>
				</div>
			}
		</div>
	</div>
</section>